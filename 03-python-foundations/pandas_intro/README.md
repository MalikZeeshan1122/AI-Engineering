# pandas intro (optional dependency)

## Purpose

Tabular ergonomics (**`DataFrame`**, **`describe`**) — bridge toward notebooks that mix CSV logs with embeddings later.

## Pair with

- **[`../exercises/16-numpy-embeddings-shape.ipynb`](../exercises/16-numpy-embeddings-shape.ipynb)** (numeric vectors mindset)

## Files

| Script | Notes |
|--------|--------|
| **`explore_frame.py`** | Builds a toy metrics frame + **`describe()`** |

## Run

```bash
pip install pandas
cd 03-python-foundations
python pandas_intro/explore_frame.py
```

If **`pandas`** is missing, the script prints install instructions and exits **0**.
