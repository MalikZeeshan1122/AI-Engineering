"""Hard-tier stubs (still bounded Python interview scope)."""

from __future__ import annotations


def trap_rain_water(height: list[int]) -> int:
    """Trapping rainwater between bars."""
    raise NotImplementedError


def word_break(s: str, word_dict: list[str]) -> bool:
    """Can s be segmented into words from word_dict (reuse allowed)?"""
    raise NotImplementedError


def merge_k_sorted_lists(lists: list[list[int]]) -> list[int]:
    """Merge ascending lists into one ascending list (heap / divide-conquer OK)."""
    raise NotImplementedError
