"""Easy-tier stubs: replace each body with your solution."""

from __future__ import annotations


def two_sum(nums: list[int], target: int) -> list[int]:
    """Return 0-based indices i != j with nums[i] + nums[j] == target (exactly one solution exists)."""
    raise NotImplementedError


def is_valid_parentheses(s: str) -> bool:
    """Balanced (), {}, [] only; empty string is valid."""
    raise NotImplementedError


def merge_sorted_lists(a: list[int], b: list[int]) -> list[int]:
    """Merge two ascending lists into a new ascending list."""
    raise NotImplementedError


def max_subarray_sum(nums: list[int]) -> int:
    """Maximum contiguous subarray sum (Kadane); nums non-empty."""
    raise NotImplementedError


def max_profit_one_transaction(prices: list[int]) -> int:
    """Best profit from one buy then sell; prices non-empty; sell after buy."""
    raise NotImplementedError


def contains_duplicate(nums: list[int]) -> bool:
    """True if any value appears more than once."""
    raise NotImplementedError


def is_anagram(s: str, t: str) -> bool:
    """Same multiset of letters (ignore case-insensitivity requirement — lowercase letters only here)."""
    raise NotImplementedError


def binary_search(nums: list[int], target: int) -> int:
    """Ascending nums; return index of target or -1."""
    raise NotImplementedError


def single_number_xor(nums: list[int]) -> int:
    """Every element appears twice except one — find it using linear time / O(1) extra."""
    raise NotImplementedError


def climb_stairs(n: int) -> int:
    """Ways to climb n stairs taking 1 or 2 steps at a time; n >= 1."""
    raise NotImplementedError
