# Decorator examples

Pairs with **`../exercises/07-decorators-generators.ipynb`**. Runnable demos:

```bash
cd 03-python-foundations
python decorators/timed_call.py
python decorators/retry_with_backoff.py
```

stdlib only (**`functools`**, **`time`**, **`random`**).
